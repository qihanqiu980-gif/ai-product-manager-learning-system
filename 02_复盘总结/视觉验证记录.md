# HTML 与 PDF 视觉验证记录

验证日期：2026-08-06

验证对象：AI 产品经理转型学习系统 v1

## 1. HTML 验证范围

- 页面：学习总览、主链路、两周突击线、十二周长期线、复盘总结；
- 桌面视口：1440 × 1000；
- 移动视口：390 × 844；
- 本地直接打开：`file://`；
- 打印媒体：A4 打印样式；
- 动效偏好：正常与 `prefers-reduced-motion: reduce`。

## 2. HTML 验证结果

- 5 个页面均只有一个主标题，`lang="zh-CN"` 正确；
- 桌面与移动端正文无横向溢出；
- 所有本地导航、PDF 链接和图片路径有效；
- 浏览器控制台无错误，GSAP 与 ScrollTrigger 本地加载正常；
- 双轨切换可正确显示 A/B 路线；
- 复盘“速览/完整”模式切换正常；
- 键盘首次 Tab 可聚焦并显示“跳到主要内容”；
- 减少动态模式下无活动动画，正文保持可见；
- 打印模式同时展开双轨内容，隐藏导航和交互控件，背景恢复为白色；
- Impeccable 检测未发现警告级反模式；保留的编号只用于真实的天数、周数和文档目录。

## 3. 已修复问题

1. 滚动入场动画曾使用透明度，未滚动时会影响整页静态截图；已改为只做轻微位移，任何状态下正文都可读。
2. PDF 深色流程条中的文字对比度不足；已改为白色文字并重新导出。
3. 390px 移动端首屏插图受 `figure` 默认外边距影响而裁切；已显式清零外边距。
4. 移动端成果映射表需要横向查看；已增加“可左右滑动”提示。
5. 小号辅助文字对比度不足；已加深 muted 色值，达到普通文字 4.5:1 以上的目标。
6. 首页主标题曾出现“出 / 发”的生硬断行；已收紧至安全字距并锁定“从产品经验出发”短语完整换行。

## 4. PDF 验证结果

- 文件：`导出PDF/AI产品经理转型学习项目-关键共识与双轨计划-v1.pdf`；
- 规格：A4，8 页；
- 使用 STSongti 中文字体，中文可正常显示和提取；
- 已将全部 8 页渲染为 PNG 逐页检查；
- 未发现文字裁切、重叠、缺字、黑块、异常分页或表格越界；
- 页眉、页脚、页码和章节切换一致；
- 封面、计划表、掌握状态、真实性红线与下一步均清晰可读。

## 5. 最终结论

HTML 与 PDF 均达到本地浏览、移动端阅读、减少动态、打印和长期复盘的第一版交付标准。本次没有创建等待回答的测验记录，也没有开始两周突击线第 1 天课程。

## 6. 2026-08-09｜Day 01—03 独立学习页增量验证

- 新增页面：Day 01 已完成历史复盘、Day 02 已完成历史复盘、Day 03 当前学习页；三页均提供互相进入与返回两周突击线总览的导航。
- 桌面验证：1440 × 1000；移动验证：390 × 844；三页均只有一个 `h1`，无正文级横向溢出，标题无容器溢出。
- Day 01/02：确认无 textarea、提交练习控件或新问题写入逻辑；本地个人原始记录默认折叠。
- Day 03 初始状态：只显示“开始前说明”，JSON Schema 节点禁用；完成 Few-shot 原理、范例练习与无占位符的 Prompt 草稿后才解锁 Schema。
- Day 03 交互：短答案被拦截；提交后才显示对照标准；字段设计、有限重试、Schema 核心检查、三项产出保存与 100% 交接页均已跑通。
- 导出验证：JSON 导出按钮生成完成反馈；页面明确提示导出文件需要由用户手动移动或交给 Codex 回写，未声称浏览器可直接修改本地状态文件。
- 浏览器控制台在修正会话 ID 显示选择器后未产生新的页面错误。

## 7. 2026-08-09｜Day 04—05 学习页与五日导航增量验证

- 新增页面：Day 04“状态机与能力覆盖”、Day 05“Rubric 与证据链”；Day 01—05 顶部日期导航均可互相进入，两周突击线总览已增加两个页面入口。
- 状态边界：验证时 Day 03 正式会话仍在进行，Day 04/05 概念仍为“未测试”；两个新页面只写浏览器 localStorage 与导出文件，没有直接修改正式学习状态。提前结束二次确认继续明确延期到十二周长期线第 1 周。
- 短屏桌面：1428 × 644；Day 04 状态机节点高度约 557px，Day 05 Rubric 节点高度约 550px，滚动到学习节点后均可在单屏内读完主要内容；标题与导航无容器溢出。
- 常规桌面：1440 × 900；柔和绿色渐变、灰绿色状态条与选中 Tab、中文系统字体、按钮层级和紧凑密度与 Day 03 一致。
- 移动端：390 × 844；Day 03—05 与历史页均无正文级横向溢出。五日导航改为组件内部横向滑动，不扩大页面宽度；标题、正文、状态条和学习节点均落在可视宽度内。
- 节点与反馈：Day 04/05 初始只解锁第一个节点；短答案被拦截且反馈保持隐藏；有效作答提交后显示对照标准；刷新后恢复答案、当前节点、反馈和进度。
- 完整流程：Day 04、Day 05 均跑通至 100% 交接页；产出模板的占位项与最低完整度检查有效，完成后可进入导出节点。
- 导出验证：Day 03、Day 04、Day 05 的 JSON 与 Markdown 均实际生成到浏览器下载目录，并已检查 JSON 字段、概念顺序、正式状态说明、练习原文、交付物和同步提示。
- Day 03 回归：在并行更新后的当前版本上重新跑通节点锁定、作答诊断、刷新恢复、Schema/重试链路、三项产出和 JSON/Markdown 导出；五日导航未破坏原功能。
- 控制台：Day 03—05 无新增 error/warn；课程总览已避免向 GSAP 传入空 NodeList，并修复深色下一步区域中 quiet 按钮的文字对比度。
